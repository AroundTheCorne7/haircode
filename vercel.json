{
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @haircode/engine build && pnpm --filter @haircode/web build",
  "outputDirectory": "apps/web/.next",
  "rewrites": [
    {
      "source": "/api/v1/:path*",
      "destination": "https://haircode-api.onrender.com/api/v1/:path*"
    }
  ]
}
